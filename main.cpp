#include "queryProcessor.h"
// this is the user interface for the search engine / the query handler

int main()
{
    queryProcessor mainQueryProcessor;

    return mainQueryProcessor.startEngine();
}
